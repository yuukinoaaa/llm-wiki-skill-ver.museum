# llm-wiki-skill 复现说明

这份文档说明：代码是怎么来的、整体逻辑是什么，以及如何从最开始只有一个 `SKILL.md` 的 skill 演进成现在的“文档摄入 + Quartz Wiki 生成”工具链。

## 1. 最初状态

项目最开始主要是一个 Claude Code skill：

- `SKILL.md`：描述 `/llm-wiki` 应该如何工作。
- `README.md` / `README.zh.md`：说明安装和使用方式。
- `translate_wiki.py`：早期用于批量翻译已有页面。
- `quartz-components/`：Quartz 的双语切换组件。

问题是：只有 skill 说明时，很多步骤依赖 Claude Code 手工执行，流程不稳定，也不方便测试。例如：

- 文档解析没有统一格式。
- 页面写入容易漏链接。
- 断链需要人工发现。
- 翻译逻辑默认开启，不适合中文讲解词。
- 本地 HTML 预览会遇到 Quartz clean URL 的 404 问题。

所以后来把“机械、可测试的部分”从 skill 中抽出来，做成 Python 工具。

## 2. 当前架构

现在项目分成两层：

### Claude Code / Skill 层

负责理解内容、拆分知识结构、生成摄入计划。

主要文件：

- `SKILL.md`
- `README.zh.md`

Claude Code 应先阅读本目录的 `SKILL.md`，再根据 `*.blocks.json` 生成 `ingest-plan.json`。

### Python 工具层

负责稳定执行，不做内容理解。

主要目录：

```text
llm_wiki_ingest/
  extractors.py    # 把 DOCX/PDF/MD/TXT 抽取成 blocks JSON
  materialize.py   # 把 ingest-plan.json 写成 Quartz Markdown 页面
  validate.py      # 检查断链、frontmatter、manifest
  serve.py         # 本地预览 Quartz public，支持 clean URL
  cli.py           # 命令行入口逻辑
  models.py        # 数据结构
  slug.py          # slug 生成
ingest_wiki.py     # 最外层命令入口
```

## 3. 核心业务逻辑

完整流程如下：

```text
源文档
  ↓ extract
*.blocks.json
  ↓ Claude Code 根据 SKILL.md 生成
ingest-plan.json
  ↓ materialize
wiki/content/*.md + llm-wiki-manifest.json
  ↓ validate
确认没有断链
  ↓ npx quartz build
wiki/public/*.html
  ↓ serve
本地浏览 HTML
```

每一步职责固定：

- `extract` 只抽文本，不判断页面结构。
- Claude Code 只负责内容理解和计划生成。
- `materialize` 只按计划写页面、补导航、补反链、补缺失实体占位页、补首页。
- `validate` 只做校验。
- `serve` 只负责本地预览 HTML。

## 4. 如何复现一次完整流程

### 1. 安装依赖

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 2. 抽取文档

把真实文件名替换进去：

```powershell
python ingest_wiki.py extract ".\展览讲解词.docx" --out ".\展览讲解词.blocks.json"
```

如果文件名不确定：

```powershell
Get-ChildItem -File -Include *.docx,*.pdf,*.md,*.txt
```

### 3. 让 Claude Code 生成摄入计划

在仓库根目录打开 Claude Code，并提示：

```text
请使用当前仓库中的 SKILL.md，按 /llm-wiki 文档摄入流程工作。
请阅读 SKILL.md、README.zh.md 和 展览讲解词.blocks.json。
先输出摄入计划摘要，列出 stop 页面、实体页面、关键互链和歧义点。
我确认后，再输出完整 ingest-plan.json。
```

保存为：

```text
ingest-plan.json
```

### 4. Dry-run

```powershell
python ingest_wiki.py materialize ".\ingest-plan.json" --wiki ".\wiki" --dry-run
```

### 5. 写入 Wiki

```powershell
python ingest_wiki.py materialize ".\ingest-plan.json" --wiki ".\wiki" --apply
```

### 6. 校验

```powershell
python ingest_wiki.py validate --wiki ".\wiki"
```

成功时输出：

```text
Validation passed
```

### 7. 构建 HTML

```powershell
cd .\wiki
npx quartz build
cd ..
```

### 8. 本地打开

不要用普通 `python -m http.server`。用项目提供的 clean URL server：

```powershell
python ingest_wiki.py serve --wiki ".\wiki" --port 8888
```

浏览器打开：

```text
http://127.0.0.1:8888/
```

## 5. 从单个 skill 到现在的演进

演进过程可以概括为四步：

1. **需求对齐**  
   明确目标不是只生成一次 HTML，而是做一个可重复摄入文档的 LLM Wiki 工具。

2. **拆职责**  
   把 LLM 擅长的“理解、拆分、写作”留给 Claude Code；把“解析、写入、校验、预览”交给 Python。

3. **脚本化**  
   新增 `llm_wiki_ingest/` 和 `ingest_wiki.py`，把流程变成命令：

   ```powershell
   extract -> materialize -> validate -> serve
   ```

4. **用问题反推完善**  
   后续遇到的问题都被固化进代码和测试：

   - `source.docx` 不存在时不再 traceback，而是提示真实文件路径。
   - `type: concepts` 这类复数类型会自动兼容。
   - `outgoing_links` 指向缺失页面时会生成占位页，避免断链。
   - 没有首页时自动生成 `index.md`。
   - Quartz clean URL 本地预览 404 时，新增 `serve` 子命令。

## 6. 隐私边界

这些文件不提交到 GitHub：

- 原始讲解词 `.docx`
- `*.blocks.json`
- `ingest-plan.json`
- 本地 `wiki/`
- `wiki/public/`

仓库只同步工具代码、skill、测试、README 和计划文档。

## 7. 当前验证命令

每次修改工具代码后，至少运行：

```powershell
python -m unittest discover -v
python ingest_wiki.py validate --wiki ".\wiki"
```

如果改了页面生成逻辑，再运行：

```powershell
python ingest_wiki.py materialize ".\ingest-plan.json" --wiki ".\wiki" --apply
cd .\wiki
npx quartz build
cd ..
python ingest_wiki.py serve --wiki ".\wiki" --port 8888
```
